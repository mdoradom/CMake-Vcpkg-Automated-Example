name: CMake with Vcpkg on multiple platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  install-dependencies:
    name: Install Dependencies with Vcpkg
    runs-on: ubuntu-latest
    outputs:
      vcpkg-root: ${{ steps.vcpkg.outputs.vcpkg-root }}

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Install Dependencies with Vcpkg
      uses: lukka/run-vcpkg@v11.5
      id: vcpkg

    - name: Install Ninja
      run: sudo apt-get install -y ninja-build

  build:
    needs: install-dependencies
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest
        build_type: [Release]
    env:
      VCPKG_ROOT: ${{ needs.install-dependencies.outputs.vcpkg-root }}

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set reusable strings
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_ENV"

    - name: Print VCPKG_ROOT
      run: "echo VCPKG_ROOT: $VCPKG_ROOT"

    - name: Configure CMake
      run: |
        if [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
          cmake -B ${{ env.build-output-dir }} -DCMAKE_TOOLCHAIN_FILE=${VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake --preset Linux-Release
        elif [[ "${{ matrix.os }}" == "windows-latest" ]]; then
          cmake -B ${{ env.build-output-dir }} -DCMAKE_TOOLCHAIN_FILE=${VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake --preset Windows-Release
        elif [[ "${{ matrix.os }}" == "macos-latest" ]]; then
          cmake -B ${{ env.build-output-dir }} -DCMAKE_TOOLCHAIN_FILE=${VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake --preset macOS-Release
        fi
      shell: /usr/bin/bash -e {0}
      env:
        VCPKG_ROOT: ${{ needs.install-dependencies.outputs.vcpkg-root }}
        build-output-dir: ${{ github.workspace }}/build

    - name: Build
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }}